import type { EducationAuthor } from "./types";
import { APP_NAME } from "@/lib/brand";

export const DEFAULT_AUTHOR: EducationAuthor = {
  name: `${APP_NAME} Editorial`,
  role: "Astrology education team",
};
